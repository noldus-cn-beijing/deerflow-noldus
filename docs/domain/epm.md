# 范式：高架十字迷宫 (EPM)

> **填写人**：<你的名字>
> **最后更新**：<YYYY-MM-DD>
> **文档状态**：草稿 / 待审核 / 已定稿

> **给填写者的话**：这份文档是领域知识的源头。工程会把它翻译成代码、测试、skill reference。
> 请尽量用**具体描述**代替抽象术语（例：与其说"看小鼠运动状态"，不如写"看 Distance moved 列是否 <1500 cm"）。
> 任何拿不准的地方写 "TODO: 待确认" —— 不要猜。

---

## 1. 一句话目的

<例：评估啮齿类动物对高处/开放空间的焦虑反应，是经典焦虑模型之一。>

**这份数据回答什么科学问题？**

-

---

## 2. 实验装置与协议（让工程/agent 知道"测试条件是什么样"）

| 项目 | 值 |
|------|-----|
| 标准测试时长 | <例：300 秒> |
| 装置尺寸 | <例：臂长 30cm，臂宽 5cm，离地 50cm> |
| 动物放置方式 | <例：从中心区面向某条闭合臂放入> |
| 光照条件 | <例：开放臂 100 lux，闭合臂 <10 lux> |
| 其他关键协议 | <例：每只动物测试后装置用 70% 乙醇擦拭> |

**如果协议显著偏离上面任一条，数据的可比性会受影响——这点会写进 agent 的质检提示里。**

---

## 3. EthoVision 导出字段 → 本范式实际会用哪些

> 专家填这个表的方式：**打开一个 demo 数据的轨迹文件**（例 `demo-data/DemoData/高架十字迷宫/轨迹-*.txt`），对照列名逐一标注。

| EthoVision 列名（原始） | 是否使用 | 用在哪个指标 | 备注 |
|---|---|---|---|
| `Trial time` | ✅ | 所有时间相关指标 | — |
| `X center`, `Y center` | ✅ / ❌ | — | |
| `Distance moved` | | | |
| `Velocity` | | | |
| `In zone(开放臂1 / center-point)` | | | <列名是否对得上？ethovision 中文/英文不一致请指出> |
| `In zone(开放臂2 / center-point)` | | | |
| `In zone(闭合臂1 / center-point)` | | | |
| `In zone(闭合臂2 / center-point)` | | | |
| `In zone(中心区 / center-point)` | | | |
| `Movement` (moving/not moving) | | | |
| `Rotation` | | | |
| （其他你实际会打开看的列） | | | |

**坑：** EthoVision 导出列名受用户当时建 zone 的命名影响，可能叫"Open_arm_1"也可能叫"开放臂1"。请写出**这批 demo 数据里**的真实列名。

---

## 4. 核心指标（按使用优先级排序）

> 填写顺序：先列你**每次分析必看**的指标，再列**偶尔看**的。
> 每个指标独立填写一个小节。

### 指标 1：开放臂停留时间比例 (open_arm_time_ratio)

- **计算公式**：
  ```
  (在开放臂1时长 + 在开放臂2时长) / 总测试时长
  ```
- **单位**：比例 (0–1) / 百分比
- **典型取值范围**（仅作批次质检参考，**不作判读依据**）：
  - C57BL/6J 成年雄鼠：<例 15–25%>
  - 其他品系你有经验的请补充：
- **这个指标最能反映什么**：<例：对高处/开放空间的回避程度>
- **计算时的陷阱 / 边缘情况**：
  - <例：动物从未离开中心区时这个值为 0，需单独标注，不能直接当"极端焦虑">
  -

### 指标 2：<下一个指标名>

- **计算公式**：
- **单位**：
- **典型取值范围**：
- **这个指标最能反映什么**：
- **计算时的陷阱**：

### 指标 3：...

<继续添加，建议 EPM 至少覆盖 6–8 个指标>

---

## 5. 组间解读规则（本范式的核心价值）

> **判读哲学**：不按绝对阈值。只看 control vs treatment 组间**统计显著差异** + **效应量**。
> 下表是决策树——agent 会按这个表逐条匹配生成结论。

### 5.1 两组比较（最常见场景）

| 观察到的统计结果 | 结论 | 置信度 | 必须附加的说明 |
|---|---|---|---|
| 处理组 开放臂时间 **显著 ↓** (p<0.05) + 总进入次数 **无显著差异** | 处理组表现出焦虑样行为增加 | 高 | 报告中列出 p 值、Cohen's d |
| 处理组 开放臂时间 **显著 ↓** + 总进入次数 **显著 ↓** | **不能直接下焦虑结论** | — | 优先检查运动能力（见 §6 混杂） |
| 处理组 开放臂时间 **显著 ↑** | 焦虑样行为减少 / 探索行为增加 | 中 | 需结合其他指标：是否伴随异常高活动性？ |
| 处理组 vs 对照组 所有指标 **无显著差异** | 当前数据未检测到处理效应 | — | 报告样本量、效应量、检验力分析 |
| <你还会处理的其他组合，请继续加> | | | |

### 5.2 多组比较（ANOVA 场景）

<如果你经常跑三组以上，请在这里写事后检验（post-hoc）的解读规则。否则留空>

### 5.3 "差异方向"的语言规范

agent 生成报告时需要统一用词。请填你的首选表述：

| 统计现象 | 推荐用词 | **不要用** |
|---|---|---|
| 处理组开放臂时间显著低于对照组 | <例："处理组表现出焦虑样行为增加"> | <例："处理组焦虑了"（太口语）> |
| p=0.06 | | |
| 效应量大但 p>0.05 | | |

---

## 6. 混杂因素排查清单（下结论前必须检查）

按**检查优先级**排序：

1. **运动能力** — 如果 <触发条件>，则 <如何处理>
   - 例：`distance_moved` 组间差异 >50% 时，焦虑指标不可单独解读，必须先在报告中说明运动能力差异
2. **…**
3. **…**

---

## 7. 可视化建议（agent 默认生成哪些图）

| 图类型 | 展示哪些指标 | 什么时候加 |
|---|---|---|
| Box plot | 开放臂时间比、进入次数比 | 默认必出 |
| Trajectory plot（轨迹叠加图） | 代表性个体轨迹 | 默认必出，每组挑 1 个 |
| Timeseries | 开放臂停留时间按分钟分段 | 当专家要求"看习惯化效应"时 |
| <其他> | | |

---

## 8. 报告要求与文献引用

### 必须引用的方法学文献

- <例：Pellow, S., et al. (1985). Validation of open:closed arm entries in an elevated plus-maze as a measure of anxiety in the rat. *J Neurosci Methods*, 14(3), 149–167.>
-

### 报告结构（APA 要求之外的领域惯例）

- 必须列出的统计量：<例：mean ± SEM, n, t/F 值, df, p, Cohen's d>
- 图的标注要求：<例：* p<0.05, ** p<0.01, *** p<0.001>

---

## 9. 疑难 case（你的经验，最值钱的部分）

> 这一节专门记录"一般流程之外"的专家判断。agent 照着一般规则出结果时，这些经验能救命。

- **小样本 (n<5)**：<你的处理办法>
- **品系差异**：<例：BALB/c 小鼠开放臂时间本就很低，不能直接套 C57 的经验。如何识别/处理？>
- **异常批次识别**：<什么迹象让你怀疑"这批数据出了问题"？例如所有动物都贴壁、都冻结>
- **跨实验室复现**：<你遇到过什么"按理该一样但不一样"的情况？>

---

## 10. TODO / 待确认

> 写文档过程中拿不准的任何事，全部丢这里。工程不会自动填上——它们会被 agent / 专家在后续 Office Hours 里解决。

- [ ] 这批 demo 数据的 zone 列名和我们实验室的是否一致？
- [ ]
- [ ]
