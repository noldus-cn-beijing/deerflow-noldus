# Sprint 5.7 实施文档 — Handoff Emission Validator（兜底 LLM 漏调 seal tool）

**面向**：独立实施 agent，直接执行
**对应 spec**：[2026-05-28-sprint-5.7-handoff-emission-validator-design.md](../specs/2026-05-28-sprint-5.7-handoff-emission-validator-design.md)
**作者校验日期**：2026-05-29（本文档的所有行号/符号/类型已对 dev HEAD 真实代码核验过）
**估期**：0.5 周（3.25 天）
**前置**：Sprint 0 已合（seal_*_handoff tool + handoff schema 就位）；与 Sprint 5/5.5 正交，无需等待

---

## 0. 给实施 agent 的第一性说明（先读这段，别跳）

这个 sprint 解决的不是"让 LLM 记得调 seal tool"——LLM 随机性是物理事实，改 prompt 只能降概率降不到 0。它解决的是**改变"LLM 漏调"的后果**：

- **现状（bug）**：subagent 的 LLM 在最后一轮只输出 thinking/content（如"现在封存分析结果："）但**没 emit `seal_*_handoff` tool_call** → executor 见无新 tool_call → 静默标 `COMPLETED` → handoff JSON 文件根本不存在 → 下游 chart-maker/report-writer `read_file` 读空 → 整条链断 → 只能靠 lead 事后 `read_file` 探测 + 运气自救。
- **本 sprint 后**：executor 在标 COMPLETED **之前**，对 4 个 ethoinsight 核心 subagent 强制检查"它该产的 handoff 文件在不在"。不在 → 标 `FAILED` + 明确诊断 message → lead 收到确定性失败信号 → 自动 re-dispatch（带强化 reminder）。

**一句话**：把"概率性的静默断链"转化为"确定性的可恢复失败"。抖动源头（LLM 随机）还在，破坏性后果（静默断链）被 harness 层拦死。

**实施纪律**（这是关键路径代码，错了会误伤正常 task）：
1. **TDD 强制**：先写测试再写实现（CLAUDE.md 硬规则）。
2. **validation helper 绝不能自己抛异常**（见 §3 的鲁棒性要点 R1，spec 漏了这条）。
3. **不退化基线**：agent backend 当前 **3111 passed**，改完必须 ≥3111。
4. 所有改动只在 dev，commit message 用中文。

---

## 1. spec 与真实代码的差异修正（实施前必看）

我已逐项核验 spec 的假设。**大部分正确，但有 3 处必须按本文档修正，不要照 spec 原文抄**：

| # | spec 原文 | 真实情况（已核验） | 实施怎么做 |
|---|---|---|---|
| **C1** | §2.3 `_resolve_workspace_path` 写 `if not isinstance(thread_data, dict)` + 自己实现取值 | `self.thread_data` 类型是 `ThreadDataState`（`thread_state.py:10` 是 **`TypedDict`**，运行时即 dict）。且 deerflow **已有现成函数** `resolve_workspace_from_state(state)` 在 `experiment_context.py:79`，内部就是 `thread_data.get("workspace_path")` | **不要自造 `_resolve_workspace_path`**。直接 `self.thread_data.get("workspace_path")`（TypedDict 是 dict，`.get` 合法），加 `isinstance(self.thread_data, dict)` 守护。复用 > 自造（MEMORY 原则）。 |
| **C2** | §2.1 字典 key 用连字符 `"data-analyst"` | `config.name` 确实是连字符（`code-executor`/`data-analyst`/`chart-maker`/`report-writer`），但 `HANDOFF_FILE_REGISTRY`（`handoff_registry.py:15`）的 key 用**下划线** `data_analyst` | 字典 key 用**连字符**（对齐 `config.name`）——spec 这点是对的。**别**直接拿 registry 的 key 当 `config.name` 比。文件名值与 registry 一致。 |
| **C3** | §2.2 把 validation 插在 `try_set_terminal(COMPLETED)` 处，未提异常安全 | `executor.py:723` 的 COMPLETED 在一个 **`try` 块内**（725 行是 `except Exception → FAILED`）。若 validation helper 抛异常，会被这个 except 抓住转成 FAILED——但 error message 会变成 IO 异常串，不是我们的诊断串 | helper **内部用 try/except 包裹文件检查**，任何异常 → fail-open 返回 None（见 R1）。绝不让 helper 的 IO 抖动把正常完成的 task 误判 FAILED。 |

**核验通过、可照 spec 抄的部分**（无需改）：
- ✅ `executor.py:723` == `result.try_set_terminal(SubagentStatus.COMPLETED)`（精确命中）
- ✅ `self.config` / `self.thread_data` / `self.trace_id` / `self.config.name` 全部存在（`__init__` 404-410 行）
- ✅ `try_set_terminal(status, error=...)` 签名接受 `error`（605/727 行实证）
- ✅ 4 个 handoff 文件名（registry 16-19 行）与 spec 完全一致
- ✅ COMPLETED 之外的终结路径（CANCELLED 605/618、FAILED 727）与 723 是不同代码点，不会被本改动波及

---

## 2. 文件改动清单（共 5 个文件：2 改 + 3 新建）

| 文件 | 操作 | 内容 |
|---|---|---|
| `subagents/executor.py` | 改 | 加 `_HANDOFF_EMISSION_REQUIRED` 字典 + `_validate_handoff_emitted` helper + 改 723 行调用点 |
| `agents/lead_agent/prompt.py` | 改 | 加"漏调 seal 自动 retry"规则段 |
| `tests/test_handoff_emission_validator.py` | 新建 | 10 个单元 case |
| `tests/test_executor_handoff_emission.py` | 新建 | 5 个集成 case |
| `tests/test_dogfood_handoff_emission.py` | 新建 | 1 个 dogfood 模拟 case |

全部路径前缀：`packages/agent/backend/packages/harness/deerflow/`（代码）/ `packages/agent/backend/`（测试）。

---

## 3. 详细实施

### T1 — `executor.py` 加白名单字典 + validator helper

**位置**：`subagents/executor.py` 顶部 module-level helpers 区（在 `class SubagentExecutor` 定义之前，约 line 100-130 区域，紧邻其他 module helper）。

**先确认 import**：文件顶部需要有 `from pathlib import Path`。grep 确认；没有就加。`logger` 已存在（全文件在用）。

```python
# ---------------------------------------------------------------------------
# Sprint 5.7: Handoff emission validation
#
# Ethoinsight subagents that MUST seal a handoff file before terminating
# successfully. Keys are SubagentConfig.name (hyphenated, matching config.name);
# values are the expected handoff filename in the thread workspace.
#
# General-purpose subagents (general-purpose / bash / knowledge-assistant) are
# intentionally NOT in this map — they have no seal_*_handoff contract and may
# legitimately complete without producing a handoff file.
# ---------------------------------------------------------------------------
_HANDOFF_EMISSION_REQUIRED: dict[str, str] = {
    "code-executor": "handoff_code_executor.json",
    "data-analyst": "handoff_data_analyst.json",
    "chart-maker": "handoff_chart_maker.json",
    "report-writer": "handoff_report_writer.json",
}


def _validate_handoff_emitted(
    subagent_name: str,
    workspace_path: str | None,
) -> str | None:
    """Return None if the subagent's handoff file is present, else a diagnostic.

    Called just before set_terminal(COMPLETED). If subagent_name is in
    _HANDOFF_EMISSION_REQUIRED but its handoff file is absent in workspace,
    returns an error string explaining the failure (used as
    try_set_terminal(FAILED, error=...)). Otherwise returns None (pass / N/A).

    ROBUSTNESS (Sprint 5.7 spec §1 C3): this function MUST NOT raise. The call
    site sits inside executor's try/except — any exception here would be caught
    and mis-attributed as a generic FAILED, clobbering our diagnostic. So all
    filesystem access is wrapped; on unexpected error we fail-open (return None).

    Args:
        subagent_name: SubagentConfig.name, e.g. "data-analyst".
        workspace_path: Host-side workspace dir from thread_data["workspace_path"].
            None when ThreadDataMiddleware didn't set it (old threads / dev paths).

    Returns:
        None  -> validation passes, or subagent not in white-list, or
                 unresolvable workspace (fail-open).
        str   -> diagnostic explaining the missing handoff.
    """
    expected_filename = _HANDOFF_EMISSION_REQUIRED.get(subagent_name)
    if expected_filename is None:
        # Not a handoff-producing ethoinsight subagent — no contract to enforce.
        return None

    if not workspace_path:
        # No workspace resolvable — can't validate. Fail-open (do NOT block);
        # this preserves pre-5.7 behavior for old threads and dev paths.
        logger.warning(
            "[handoff_emission] Subagent %s: no workspace_path; skipping "
            "handoff validation (fail-open).",
            subagent_name,
        )
        return None

    try:
        handoff_path = Path(workspace_path) / expected_filename
        if handoff_path.exists():
            return None
    except Exception:  # noqa: BLE001 — must never raise; see docstring ROBUSTNESS.
        logger.exception(
            "[handoff_emission] Subagent %s: error while checking %s; "
            "failing open.",
            subagent_name,
            expected_filename,
        )
        return None

    seal_tool = f"seal_{subagent_name.replace('-', '_')}_handoff"
    return (
        f"Subagent '{subagent_name}' terminated without emitting "
        f"'{expected_filename}'. This means the LLM finished its reasoning "
        f"but forgot to call the {seal_tool} tool. The handoff file is "
        f"REQUIRED by downstream subagents. Lead should re-dispatch this task "
        f"with an explicit reminder to invoke the seal tool at the end."
    )
```

**诊断串的固定关键字**（测试和 lead prompt 都依赖，不能改）：
- `terminated without emitting`（lead retry 规则靠这个识别）
- `{expected_filename}`（如 `handoff_data_analyst.json`）
- `seal_{name}_handoff`（如 `seal_data_analyst_handoff`）
- `Lead should re-dispatch`

### T2 — 改 723 行调用点为 validate-first

**位置**：`subagents/executor.py:723`

**改前**（精确原文）：
```python
            result.try_set_terminal(SubagentStatus.COMPLETED)
```

**改后**：
```python
            # Sprint 5.7: validate handoff emission before marking COMPLETED.
            # For ethoinsight subagents (code-executor / data-analyst /
            # chart-maker / report-writer), the expected handoff file MUST exist
            # in the thread workspace. An LLM can finish its thinking turn
            # without emitting the seal_*_handoff tool_call — executor would
            # otherwise mark the task COMPLETED falsely and break downstream.
            _workspace = (
                self.thread_data.get("workspace_path")
                if isinstance(self.thread_data, dict)
                else None
            )
            _handoff_error = _validate_handoff_emitted(self.config.name, _workspace)
            if _handoff_error is not None:
                logger.warning(
                    "[trace=%s] Subagent %s terminated without emitting handoff: %s",
                    self.trace_id,
                    self.config.name,
                    _handoff_error,
                )
                result.try_set_terminal(SubagentStatus.FAILED, error=_handoff_error)
            else:
                result.try_set_terminal(SubagentStatus.COMPLETED)
```

**关键点（C1）**：直接 `self.thread_data.get("workspace_path")`，因为 `ThreadDataState` 是 TypedDict（运行时是 dict）。**不要**新建 spec §2.3 的 `_resolve_workspace_path` method——多余，且其 `isinstance(thread_data, dict)` 注释会误导。inline 一行 + `isinstance` 守护即可。

**关键点（C3）**：这段仍在原 `try` 块内（725 行 `except` 之后跟着）。因为 `_validate_handoff_emitted` 内部已保证不抛（R1），所以这里不会把正常 task 误转 FAILED。**不要**把这段挪出 try 块——挪出去会改变 result 提取逻辑的异常语义。

### T3 — lead prompt 加自动 retry 规则

**位置**：`agents/lead_agent/prompt.py`，找到现有的"subagent 派遣后失败处置 / task failed 处理"相关段落（grep `task` + `失败` / `FAILED` / `重新派遣` 定位）。在该段加：

```
- **subagent 漏调 seal tool 的自动重试规则**（Sprint 5.7 harness 兜底）:
  当收到 task failed 且 error message 含 "terminated without emitting" 关键字时,
  这是 harness 层检测到 subagent 的 LLM 完成了推理但漏调 seal_*_handoff tool 的
  明确信号,**不需要询问用户**,直接重新派遣同一个 subagent。在新派遣的 prompt
  末尾追加一句强化提示:
    "提醒:你必须在完成分析后调用 seal_<subagent_type>_handoff tool 才能落库
     handoff JSON,不能只在 thinking 里写'封存'或'已完成'。"
  同一 subagent 最多自动重试 2 次;若 2 次后仍报同样错误,向用户报告:
    "<subagent> 多次未能正确封存结果,可能是 prompt 配置问题,建议人工检查。"
```

**注意**（deepseek 正面提示原则，CLAUDE.md §6）：用"你必须调用 X tool 才能落库"这种**正面指令**，不要写"不要忘记""禁止只写 thinking"——会反向激活。上面措辞已遵守。

---

## 4. 测试（TDD：先写测试，跑红，再实现）

### T4 — `tests/test_handoff_emission_validator.py`（10 单元 case）

直接测 module-level `_validate_handoff_emitted`（不需要构造 SubagentExecutor，纯函数）。用 `tmp_path` fixture 造 workspace。

```python
from pathlib import Path
import pytest
from deerflow.subagents.executor import _validate_handoff_emitted

def _make_workspace(tmp_path: Path, *, with_file: str | None = None) -> str:
    ws = tmp_path / "workspace"
    ws.mkdir()
    if with_file:
        (ws / with_file).write_text("{}", encoding="utf-8")
    return str(ws)
```

| 测试函数 | 构造 | 断言 |
|---|---|---|
| `test_data_analyst_handoff_present_passes` | workspace 含 `handoff_data_analyst.json` | 返回 `None` |
| `test_data_analyst_handoff_absent_returns_diagnostic` | workspace 空 | 返回 str，且包含 `"data-analyst"`、`"handoff_data_analyst.json"`、`"seal_data_analyst_handoff"`、`"terminated without emitting"`、`"Lead should re-dispatch"` |
| `test_code_executor_validates_same_way` | `code-executor` + 无 `handoff_code_executor.json` | 返回 str 含 `seal_code_executor_handoff` |
| `test_chart_maker_validates_same_way` | `chart-maker` + 无 `handoff_chart_maker.json` | 返回 str 含 `seal_chart_maker_handoff` |
| `test_report_writer_validates_same_way` | `report-writer` + 无 `handoff_report_writer.json` | 返回 str 含 `seal_report_writer_handoff` |
| `test_general_purpose_skipped` | `general-purpose` + 空 workspace | 返回 `None`（不在白名单） |
| `test_bash_skipped` | `bash` + 空 workspace | 返回 `None` |
| `test_knowledge_assistant_skipped` | `knowledge-assistant` + 空 workspace | 返回 `None` |
| `test_no_workspace_path_skipped` | `data-analyst` + `workspace_path=None` | 返回 `None`（fail-open） |
| `test_empty_workspace_path_skipped` | `data-analyst` + `workspace_path=""` | 返回 `None` |

**额外建议加一个 C3 鲁棒性 case**（spec 没列，但这是修正点，值得守护）：
| `test_helper_never_raises_on_bad_path` | `workspace_path` 传一个会让 `Path(...).exists()` 出错的畸形值（如包含 null byte 的串 `"\x00bad"`）| 不抛异常，返回 `None` |

### T5 — `tests/test_executor_handoff_emission.py`（5 集成 case）

测 `SubagentExecutor` 终结路径。**实施前先看 `tests/conftest.py`**——spec/CLAUDE 提到 executor 可能需要 `sys.modules` mock（已有先例）。优先复用现有 executor 测试的构造方式（grep `tests/` 里现有 `SubagentExecutor(` 用例，照搬 fixture）。

| 测试函数 | 验证 |
|---|---|
| `test_completed_subagent_with_handoff_passes` | data-analyst task 完成 + workspace 有 `handoff_data_analyst.json` → 最终 `status == COMPLETED` |
| `test_completed_subagent_without_handoff_marked_failed` | data-analyst 完成 + workspace 无 handoff → `status == FAILED` 且 `error` 含 `"terminated without emitting"` |
| `test_completed_general_purpose_without_handoff_still_passes` | general-purpose + 空 workspace → `COMPLETED`（白名单外不验证） |
| `test_cancelled_path_skips_validation` | task 被 cancel（走 605/618 行 CANCELLED）→ `status == CANCELLED`，**不**被 5.7 逻辑改成 FAILED |
| `test_failed_path_skips_validation` | task 自身抛异常（走 727 行）→ `status == FAILED` 且 error 是**原异常串**，不被 handoff 诊断覆盖 |

> 集成测试若构造完整 SubagentExecutor 成本过高，允许退化为：直接构造 `SubagentResult`，手动走"提取 result → 调 validate → set_terminal"这段逻辑的等价路径。重点是覆盖 **COMPLETED/FAILED/CANCELLED 三条终结路径与 validation 的交互**，而非完整跑 LLM。

### T6 — `tests/test_dogfood_handoff_emission.py`（1 模拟 case）

| 测试函数 | 验证 |
|---|---|
| `test_simulated_llm_forgets_seal_tool` | 模拟"LLM 最后一条 AIMessage 只有 content（如 `"现在封存分析结果："`）无 tool_call"且 workspace 无 `handoff_data_analyst.json` → 终结路径标 `FAILED` + error 串可被 lead retry 规则的 `"terminated without emitting"` 关键字命中 |

这个 case 是把 5-28 真实 dogfood（thread `68f6da40...` row 14）的失败现场固化成回归测试。

---

## 5. 实施顺序（task 拆分）

| Task | 内容 | 估时 | 产物 |
|---|---|---|---|
| T0 | grep 确认 `from pathlib import Path` 在 executor.py；看 conftest.py 现有 executor mock | 0.1 天 | — |
| T4 | 先写 `test_handoff_emission_validator.py`（11 case，含 C3）→ 跑红 | 0.4 天 | 红 |
| T1 | executor.py 加字典 + helper → T4 转绿 | 0.25 天 | T4 绿 |
| T2/T3 | 改 723 行调用点（inline workspace 取值，**不**建 `_resolve_workspace_path`）| 0.25 天 | — |
| T5 | 集成测试 5 case | 0.5 天 | 绿 |
| T6 | dogfood 模拟 1 case | 0.25 天 | 绿 |
| T7 | lead prompt retry 规则段（§3 T3）| 0.25 天 | — |
| T8 | 全量回归 `make test`，agent backend ≥3111 不退化 | 0.25 天 | ≥3111 |
| T9 | **dogfood 真机验证**（见 §6）| 0.5 天 | — |
| T10 | 清理临时 dogfood 改动 + commit + 写交接 | 0.25 天 | commit |
| **合计** | | **~3.25 天** | |

> 注意 TDD 顺序：T4 测试**先于** T1 实现。

---

## 6. dogfood 真机验收（T9，必须做）

**目的**：证明这张网真的接得住，而不只是单测绿。

**方法**（spec §5）：
1. 临时改 `subagents/builtins/data_analyst.py` 的 workflow，**故意把 step 2.7 的 seal 指令去掉/弱化**，人为复现"LLM 漏调 seal"的高概率场景（commit `2df10880` 修的就是这个；这里临时 revert 它的效果）。
2. `make dev` 启动，上传 EPM n=1 数据，跑完整 E2E。
3. **期望观察**：
   - data-analyst 第一次派遣 → executor 标 **FAILED**（不再静默 COMPLETED）；
   - lead 收到含 `"terminated without emitting"` 的 error → **自动**重新派遣（不询问用户）；
   - 第二次派遣 prompt 末尾带强化 reminder → 大概率成功产 `handoff_data_analyst.json`；
   - 整条链跑通到 report-writer，**用户视角无内部错误诊断泄漏**。
4. **关键**：跑完**立即把第 1 步的临时改动 revert 干净**（T10），不要把削弱版 prompt 留在 dev。

**对比基线**：5-28 那次 dogfood 是 executor 静默 COMPLETED + lead 靠 `read_file` 探测自救。本次应是 executor 主动 FAILED + lead 按规则确定性 retry。这个行为差异就是 5.7 是否生效的判据。

---

## 7. 验收 checklist（合并前逐条核对）

- [ ] `executor.py` 有 `_HANDOFF_EMISSION_REQUIRED`（4 个连字符 key）+ `_validate_handoff_emitted`（含 R1 不抛异常保证）
- [ ] 723 行改为 validate-first；workspace 取值用 inline `self.thread_data.get("workspace_path")` + isinstance 守护（**未**新建 `_resolve_workspace_path`）
- [ ] CANCELLED（605/618）/ 异常 FAILED（727）路径**未**被波及，原 error 保留
- [ ] lead prompt 含 `"terminated without emitting"` 自动 retry 规则（正面指令措辞，最多 2 次）
- [ ] 单元 11 case + 集成 5 case + dogfood 模拟 1 case 全绿
- [ ] dogfood 真机：临时削弱 data_analyst step 2.7 → 第一次 FAILED → lead 自动 retry → 第二次成功 → **临时改动已 revert**
- [ ] `make test` 全量 ≥3111 不退化
- [ ] `git grep "_HANDOFF_EMISSION_REQUIRED\|terminated without emitting"` 在 executor.py + 测试 + prompt.py 都能命中
- [ ] commit message 中文；只在 dev

---

## 8. 不在本 sprint 范围（防 scope creep）

- ❌ general-purpose / bash / knowledge-assistant 的产物兜底（无 seal contract）
- ❌ 校验 handoff **内容**一致性（那是 Sprint 5.5 lineage sha256）
- ❌ LLM 在 seal tool 内传错 schema 的兜底（Sprint 0 _seal_handoff 已 raise）
- ❌ 改 task_tool.py 的 polling / SSE
- ❌ 给 subagent prompt 加更多"记得调 seal"的废话（本 sprint 是 harness 兜底，prompt 只加 retry 规则）

---

## 9. 关键文件地图（已核验行号，dev HEAD）

| 用途 | 路径:行 |
|---|---|
| 改：终结调用点 | `packages/agent/backend/packages/harness/deerflow/subagents/executor.py:723` |
| 参考：`__init__` 收 thread_data | 同上 `:372-412` |
| 参考：CANCELLED/FAILED 既有路径 | 同上 `:605` `:618` `:727` |
| 参考：`try_set_terminal` 签名 | 同上 `:128` |
| 复用：workspace 取值现成函数 | `agents/middlewares/experiment_context.py:79` `resolve_workspace_from_state` |
| 核验：workspace_path 字段 | `agents/thread_state.py:11`（`ThreadDataState` TypedDict） |
| 核验：handoff 文件名 | `subagents/handoff_registry.py:15-19`（key 用下划线，值是文件名） |
| 改：lead retry 规则 | `agents/lead_agent/prompt.py`（grep task 失败处置段） |
| 实证 bug 现场 | `.deer-flow/training-data/auto-collected/68f6da40-6c62-494e-922c-8e3c82c156b5.jsonl` row 14 |

---

## 10. 与 Sprint 5.5 协同（背景，不影响本 sprint 实施）

| | Sprint 5.7（本） | Sprint 5.5 |
|---|---|---|
| 管什么 | handoff **是否存在**（emission） | handoff **内容一致性**（sha256） |
| 触发点 | 当前 subagent 终结**前**（executor 内） | 下游派遣**前**（guardrail pre-tool-call） |
| 失败处置 | task 标 FAILED → lead retry | guardrail deny → lead 收拒绝 |

5.7 是 5.5 的前提（先确保文件在，再校验内容）。两者独立可合，无先后强依赖。
